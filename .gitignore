## .NET
bin/
obj/
*.user

## Real local secrets — never commit. appsettings.json now holds real local values and is untracked
## from here on (it was committed once with empty placeholders — that commit is unaffected).
its-a-date-project/appsettings.json
appsettings.Development.json
appsettings.*.local.json

## Visual Studio
.vs/

## Rider / VS Code
.idea/
.vscode/

## OS
.DS_Store
Thumbs.db
